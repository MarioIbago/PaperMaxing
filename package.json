{
  "name": "papermaxing",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm --workspace @papermaxing/web run dev",
    "build": "npm --workspace @papermaxing/web run build",
    "start": "npm --workspace @papermaxing/web run start"
  },
  "engines": { "node": ">=22" }
}
