import {NextResponse} from "next/server";import {getNotebookLMProvider} from "../../../../../src/lib/providers/notebooklm";export const runtime="nodejs";export const dynamic="force-dynamic";export async function GET(){try{const health=await getNotebookLMProvider().health();return NextResponse.json({provider:"notebooklm",...health});}catch(error){return NextResponse.json({provider:"notebooklm",ok:false,error:error instanceof Error?error.message:"Provider unavailable"},{status:503});}}
