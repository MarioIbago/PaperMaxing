"use client";

import { useMemo, useState } from "react";
import { Icon } from "../components/icons";
import { MobileBottomNav, PageFrame } from "../components/shell";
import { comparisonRows } from "../components/data";
import { GreekDivider, Tag } from "../components/ui";

const papers = ["Vaswani et al., 2017", "Devlin et al., 2018", "Liu et al., 2019"];
const paperTitles = ["Attention Is All You Need", "BERT: Pre-training of Deep Bidirectional Transformers", "RoBERTa: A Robustly Optimized BERT Pretraining Approach"];
const dimensions = ["Question", "Method", "Data", "Results", "Limits", "Conclusion"];

function RelationshipTag({ value }: { value: string }) {
  const config = value === "Supports" ? ["green", "check"] : value === "Contradicts" ? ["red", "close"] : value === "Extends" ? ["violet", "star"] : ["gold", "info"];
  return <Tag tone={config[0] as "green" | "red" | "violet" | "gold"} icon={config[1] as never}>{value}</Tag>;
}
export default function ComparePage() {
  const [dimension, setDimension] = useState("Question");
  const row = useMemo(() => comparisonRows.find((item) => item.label.toLowerCase().startsWith(dimension.toLowerCase().slice(0, 4))) ?? comparisonRows[0], [dimension]);
  return <PageFrame active="product" className="compare-page"><main className="compare-main"><section className="compare-heading"><div><h1>❧ Compare Papers ❧</h1><p>Side-by-side comparison to synthesize findings and surface insights.</p></div><div className="compare-selection"><div className="compare-selection-top"><strong>3 papers selected <span>3</span></strong><div><button type="button" className="button button-outline"><Icon name="arrow" size={16} /> Align claims</button><button type="button" className="button button-primary"><Icon name="download" size={16} /> Export comparison</button></div></div><div className="paper-chips">{papers.map((paper) => <span key={paper}>{paper}<button type="button" aria-label={`Remove ${paper}`}><Icon name="close" size={13} /></button></span>)}<button type="button" className="add-paper"><Icon name="plus" size={14} /> Add paper</button></div></div></section><section className="mobile-dimension-tabs">{dimensions.map((item) => <button key={item} className={dimension === item ? "is-active" : ""} type="button" onClick={() => setDimension(item)}>{item}</button>)}</section><div className="compare-grid"><section className="comparison-matrix paper-card"><div className="matrix-header"><span />{papers.map((paper, index) => <div key={paper}><strong>{paper} <Icon name="external" size={11} /></strong><small>{paperTitles[index]}</small></div>)}</div>{comparisonRows.map((item) => <div className={`matrix-row ${item.label === row.label ? "mobile-is-active" : ""}`} key={item.label}><div className="matrix-label"><Icon name={item.icon} size={20} /><span>{item.label}</span></div>{item.values.map((value, index) => <div className="matrix-value" key={index}><p>{value}</p>{item.tags?.[index] ? <RelationshipTag value={item.tags[index]} /> : null}</div>)}</div>)}<div className="matrix-relationship"><div className="matrix-label"><Icon name="balance" size={20} /><span>Overall relationship</span></div><div><Tag tone="green">Foundational</Tag><p>Introduces the core architecture that makes the rest possible.</p></div><Icon name="arrow" size={26} /><div><Tag tone="violet">Builds on</Tag><p>Applies the architecture with pre-training for strong transfer.</p></div><Icon name="arrow" size={26} /><div><Tag tone="violet">Extends</Tag><p>Improves the approach through scale and optimization.</p></div></div></section><aside className="disagreement-panel paper-card"><h2>Why do these papers disagree?</h2><GreekDivider />{[["Population", "Different corpora and domains (e.g., BooksCorpus vs. CC-News) lead to different generalization.", "users"], ["Method", "Differences in pre-training objectives (NSP vs. none), masking strategies, and model sizes.", "gear"], ["Period", "Advances over time (2017 → 2018 → 2019) bring more data, compute, and better practices.", "calendar"], ["Measurement", "Benchmarks evolve (e.g., GLUE versions), making direct comparisons imperfect.", "tag"], ["Assumptions", "Assumes more data and compute monotonically improve performance.", "target"], ["Statistical Power", "Larger datasets and longer training improve confidence in reported gains.", "chart"]].map(([label, text, icon]) => <div className="disagreement-row" key={label}><Icon name={icon as never} size={21} /><span><strong>{label}</strong><p>{text}</p></span></div>)}<div className="takeaway"><Icon name="lightbulb" size={25} /><div><strong>Takeaway</strong><p><b>Observation:</b> differences are largely due to data scale, training objectives, and evaluation choices. <b>Inference:</b> the evidence shows consistent progress building on the same foundation.</p></div></div></aside></div><div className="mobile-compare-privacy"><Icon name="shield" size={17} /> Your comparisons are private and never shared. <a href="#privacy">Learn more <Icon name="arrow" size={14} /></a></div></main><MobileBottomNav selected="more" /></PageFrame>;
}
